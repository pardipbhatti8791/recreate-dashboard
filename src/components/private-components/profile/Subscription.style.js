import styled from 'styled-components';

const SubscriptionStyleWrapper = styled.div`
  width: 100%;
  min-height: 100vh;
  height: 100%;
  /* align-items: center; */
  background: #fff;
  position: relative;
  .subscription-container{
    padding: 50px;
    padding-bottom: 20px;
  }
  .back-icon{
    font-size: 25px;
  }
`;

export default (SubscriptionStyleWrapper);
